{
  "private": true,
  "name": "@lincebi/frontend",
  "packageManager": "pnpm@8.3.1",
  "type": "module",
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "preview": "pnpm -r run preview",
    "clean": "pnpm -r run clean",
    "lint": "pnpm -r run lint",
    "format": "pnpm -r run format"
  },
  "devDependencies": {
    "@datadog/datadog-ci": "~2.10.0"
  }
}
